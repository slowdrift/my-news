# マイニュース 画面デザインの点検

画面の見た目・使い勝手を良くするときは、この手順で点検する。
IMPROVE_PROMPT.md（機能と情報の改善）の「見た目」の部分を受け持つ。

## デザインの目的

毎朝スマホで開き、**朝刊（きょうの新着）を短い時間で読み切り**、時間があれば**書庫**を眺める。
デザインはこの流れを助けるためにある。飾りのための変更はしない。

## 利用の前提

- Android のスマホ（幅 412px 前後）。片手で親指で操作する
- 朝は数分。見出しをざっと見て、読むものだけ開き、残りは片づける
- 明るい配色・暗い配色の両方を使う
- 夜は妻と食事をしながらテレビ情報を見る

## 手順

### 0. 撮る
`python tools/screenshot.py 出力.png`（暗い配色は `--dark`）で、412px 幅の画面を撮る。
30日使い続けた状態を仕込んで撮るので、NEW や「●前回から」も写る。
**直す前と直した後を同じ条件で撮り、並べて比べる。**

### 1. 最初の1画面（高さ約 900px）に何があるか
- 朝刊の件数と入口が、スクロールせずに見えるか
- 天気・ボタン・カテゴリの札など、記事以外が何 px を占めているか（数える）

### 2. 見た目の強さの順番
朝刊 ＞ 書庫 ＞ テーマ ＞ 記事 の順に目立っているか。
**同じ色・同じ形なのに意味が違う部品**がないか（例：新着と発掘が同じ橙色）。

### 3. 記事カードの情報量
- 1枚のカードに付く印・ボタンの数を数える
- 同じ意味の印が重なっていないか（例：「●前回から」と「NEW」）
- 折り返して崩れていないか（例：👎だけ次の行に落ちる）

### 4. 押しやすさ
- 押す部品の高さ（目安 44px 以上。Android の推奨は 48dp）
- 隣の部品との間隔。取り返しのつかない操作が押しやすい場所にないか

### 5. 読みやすさ
- 本文の文字は 14px 以上、行間は 1.5 前後
- 文字と背景のコントラストは WCAG AA（4.5:1）以上。**計算で確かめる**（目で見て判断しない）
- 暗い配色でも同じ基準で確かめる

### 6. 状態ごとの見た目
新着ゼロ／読み終えた／取得に失敗した／読めない記事 — それぞれ意味の分かる表示になっているか。

### 7. 言葉と形のそろい
同じ操作を同じ言葉で呼んでいるか（片づけ／既読／読んだ）。ボタンの形と色の意味がそろっているか。

## 出力の形

```
【撮影】直す前（明・暗）
【問題点】重い順。数字で（px・件数・コントラスト比）
【直し方】
【撮影】直した後（明・暗）と、変わった数字
```

## やらないこと

- 見た目のために情報を消さない（消す判断は利用者がする）
- 外部のライブラリや画像を足さない（index.html を開くだけで動くことを守る）
- 好みの問題で大きく作り替えない。目的（朝刊を読み切る）に効くものだけ直す
