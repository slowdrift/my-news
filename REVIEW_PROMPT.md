# マイニュース 改修レビュー

「レビューして」と言われたら、この手順で点検する。実装者としてではなく、検査する側として答える。

## このアプリの目的

スマホで毎朝開くだけで、**私が本当に読みたい情報に到達できる**こと。
情報源はニュースサイトに限らない。欲しいのは情報であって、収集の実績ではない。

## 絶対条件（変更不可）

- 継続コストは完全ゼロ。有料APIを使わない
- 記事の自動要約はしない
- 取得に失敗しても止まらず、取れたものだけ出す。ゼロ件はログに残す
- ローカルで `index.html` を開くだけでも表示できる

## 点検してほしいこと

### 1. 末端で数える

各改修について、「私の画面に出るところ」まで到達しているかを数字で示す。
このアプリは **集める → 貯める → 渡す(data.js) → 描く → 操作** という流れで、
途中の工程で止まっていても手前の数字は増える。工程ごとの件数を並べ、
どこで減っているかを示すこと。
**「◯◯件取得できた」は完了の根拠にならない。**

### 2. できないことを名指しする

私の要望のうち、この構成（静的サイト・サーバーなし・既読は端末のみ）では
原理的に実現できないものを挙げる。
代用の仕組みで埋めている箇所があれば、「本来の要望」と「実際の挙動」の差を書く。
ごまかさず、代わりに何ができるかを提案する。

### 3. 情報源の枠を疑う

「見つからない」と結論した対象について、探した範囲を書き出す。
ニュース検索の外（個人ブログ・出版社・専門サイト・図書館・動画・公的機関など）を
試したかを確認する。試していなければ、試してから結論を出す。

### 4. 実際の使い方で確かめる

確認は「毎朝スマホで開く」条件で行う。
テスト用に値を仕込んだ確認は、それだけでは根拠にしない。
一度開いて閉じ、翌日また開く、という流れで壊れないかを見る。

### 5. 約束と実装のズレを探す

過去に私へ説明した仕様と、いま動いているコードを突き合わせる。
食い違いがあれば、コードではなく**私への説明のほうを訂正する**。

## 出力の形

```
【要求ごとの到達度】
  要求 / 実装したこと / 画面で確認できた事実(数字) / 未達の部分

【できないこと】
  要望 / なぜ無理か / 代わりの案

【約束と実装のズレ】

【次にやるべきこと】
  優先順に。理由は「私の読書体験がどう変わるか」で書く。
```

## やらないこと

- 途中工程の数字で「完了」と報告しない
- 実現できない部分を黙って代用品に置き換えない
- 一度に多くを直そうとしない。私が確認できる単位に切る
