# Googleアラートの「秘密のURL」を設定するための見本です。
# 使い方:
#   1) このファイルの中身を、新規ファイル .env にコピー
#      （.env は .gitignore で除外され、GitHubには公開されません）
#   2) 各行の = の右側に、実際のフィードURLを貼り付け
#
# GitHub Actions では、この3つを同じ名前で「Secrets」に登録します。
# （リポジトリ Settings → Secrets and variables → Actions → New repository secret）

ALERT_SAWAKI_URL=
ALERT_AQUA_URL=
ALERT_CLAUDE_URL=
